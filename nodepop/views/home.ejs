<%- include('header.ejs') %>
<div class="container">
    <p>El segundo actual</p>
    <h2>ingrese productos usuario bucle</h2>
    <% products.forEach(product => { %>
        <p> producto: <%= product.name %> 
            price:  <%= product.price %> 
            foto: <a href=""><%= product.foto %></a> 
            tags: <% product.tags %></p>
    <% }) %>
    <h2>Maquetado</h2>
</div>

<%- include('footer.ejs') %>